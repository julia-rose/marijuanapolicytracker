<%= form_for @subscription do |f| %>
	<%= f.hidden_field :user_id, :value => current_user.id %>
	<%= f.hidden_field :state_id, :value => @state.id %>
	<%= f.submit "Subscribe to Policy Updates for #{@state.name}", class: "follow_button" %>
<% end %>
